# URL base da API (ex.: http://localhost:3001)
VITE_API_BASE_URL=http://localhost:3001

# Opcional no dev: ativa proxy do Vite para evitar CORS em ambiente local.
# Se definir este valor, o frontend pode usar paths relativos (/auth, /users, ...).
VITE_API_PROXY_TARGET=http://localhost:3001
